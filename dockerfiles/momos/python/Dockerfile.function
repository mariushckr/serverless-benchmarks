ARG BASE_IMAGE
FROM ${BASE_IMAGE}
ARG VERSION
ENV PYTHON_VERSION=${VERSION}

ARG MOMOS_IMAGE_NAME
ENV MOMOS_IMAGE_NAME=$MOMOS_IMAGE_NAME

WORKDIR /function
COPY . /function
RUN pip install --no-cache-dir -r requirements.txt

CMD ["python3", "__main__.py"]